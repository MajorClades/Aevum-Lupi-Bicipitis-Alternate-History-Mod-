PR1_1_slot = {
	slot = 1
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		OR = {
			tag = PR1
			AND = {
				tag = PER
				was_tag = PR1
			}
		}
	}

	eran_north_way = {
		icon = per_establish_eastern_defenses
		position = 2
		required_missions = { eran_one_falls } 
		provinces_to_highlight = {
			OR = {
				area = isfahan_area
				area = iraq_e_ajam_area
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT } 
		}

		trigger = {
			isfahan_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			iraq_e_ajam_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}

		effect = {
			add_yearly_manpower = 1
			isfahan_area = { change_religion = zoroastrian } 
			iraq_e_ajam_area = { change_religion = zoroastrian } 
		}
	}

	eran_push_west = {
		icon = mission_into_persia_1
		position = 3
		required_missions = {
			eran_north_way
			eran_the_other_follow
		}
		provinces_to_highlight = { province_id = 428 } 

		trigger = {
			428 = {
				owned_by = ROOT
				religion = zoroastrian
				development = 20
			}
		}

		effect = {
			428 = {
				add_great_project_tier = {
					type = golestan_palace
					tier = 2
				}
			}
		}
	}
}
PR1_2_slot = {
	slot = 2
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		OR = {
			tag = PR1
			AND = {
				tag = PER
				was_tag = PR1
			}
		}
	}

	eran_one_falls = {
		icon = per_our_religious_direction
		position = 1
		provinces_to_highlight = { is_capital_of = MD1 } 

		trigger = {
			OR = {
				is_at_war = no
				has_won_war_against = { who = MD1 } 
			}
			all_core_province = { owned_by = ROOT } 
		}

		effect = {
			add_government_power = {
				mechanic_type = firenation_mechanic
				power_type = fire_nation
				value = 10
			}
			persia_region = { add_permanent_claim = ROOT } 
		}
	}

	eran_the_other_follow = {
		icon = per_create_the_tupchi
		position = 2
		required_missions = { eran_one_falls } 
		provinces_to_highlight = {
			region = persia_region
			NOT = { country_or_non_sovereign_subject_holds = ROOT } 
		}

		trigger = {
			num_of_owned_provinces_with = {
				value = 25
				region = persia_region
				religion = zoroastrian
			}
		}

		effect = {
			capital_scope = {
				1_vinland_cannons = ROOT
				1_vinland_cannons = ROOT
				1_vinland_cannons = ROOT
				custom_tooltip = fj0_cannons_bad_tt
			}
		}
	}

	eran_the_persian_return = {
		icon = per_a_new_eranshahr
		position = 4
		required_missions = { eran_the_other_follow } 
		provinces_to_highlight = {
			region = persia_region
			NOT = { country_or_non_sovereign_subject_holds = ROOT } 
		}

		trigger = {
			num_of_provinces_owned_or_owned_by_subjects_with = {
				value = 27
				region = persia_region
			}
		}

		effect = {
			add_ruler_modifier = {
				name = "eran_shah_restorer"
				duration = -1
			}
			2212 = {
				if = {
					limit = { country_or_non_sovereign_subject_holds = ROOT } 
					add_base_tax = 1
					add_base_production = 1
					add_base_manpower = 1
				}
				else = { add_core = ROOT } 
			}
			4430 = {
				if = {
					limit = { country_or_non_sovereign_subject_holds = ROOT } 
					add_base_tax = 1
					add_base_production = 1
					add_base_manpower = 1
				}
				else = { add_core = ROOT } 
			}
			2223 = {
				if = {
					limit = { country_or_non_sovereign_subject_holds = ROOT } 
					add_base_tax = 1
					add_base_production = 1
					add_base_manpower = 1
				}
				else = { add_core = ROOT } 
			}
			441 = {
				if = {
					limit = { country_or_non_sovereign_subject_holds = ROOT } 
					add_base_tax = 1
					add_base_production = 1
					add_base_manpower = 1
				}
				else = { add_core = ROOT } 
			}
			4336 = {
				if = {
					limit = { country_or_non_sovereign_subject_holds = ROOT } 
					add_base_tax = 1
					add_base_production = 1
					add_base_manpower = 1
				}
				else = { add_core = ROOT } 
			}
		}
	}
}
PR1_3_slot = {
	slot = 3
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		OR = {
			tag = PR1
			AND = {
				tag = PER
				was_tag = PR1
			}
		}
	}

	eran_south_way = {
		icon = mam_hormuz_straits
		position = 2
		required_missions = { eran_one_falls } 
		provinces_to_highlight = {
			OR = {
				area = farsistan_area
				area = mogostan_area
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT } 
		}

		trigger = {
			farsistan_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			mogostan_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}

		effect = {
			define_admiral = {
				shock = 3
				fire = 2
				manuever = 6
			}
			1334 = { add_trade_node_income = 60 } 
		}
	}

	eran_more_stuff = {
		icon = mission_break_the_tatar_yoke
		position = 3
		required_missions = { eran_the_other_follow } 
		provinces_to_highlight = { is_capital_of = BG0 } 

		trigger = {
			BG0 = {
				OR = {
					exists = no
					is_subject_of = ROOT
				}
			}
		}

		effect = {
			add_legitimacy_or_mil_power = { amount = 20 } 
			add_power_projection_mission = { amount = 25 } 
		}
	}
}
PR1_4_slot = {
	slot = 4
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		OR = {
			tag = PR1
			AND = {
				tag = PER
				was_tag = PR1
			}
		}
	}

	eran_zoroaster_temples = {
		icon = mission_into_persia_2
		position = 2
		required_missions = { eran_two_falls } 
		provinces_to_highlight = { is_capital_of = ROOT } 

		trigger = {
			capital_scope = {
				development = 20
				has_tax_building_trigger = yes
				superregion = persia_superregion
				religion = zoroastrian
			}
		}

		effect = {
			capital_scope = {
				add_building = university
				add_or_upgrade_tax_building = yes
			}
		}
	}

	eran_fires_up = {
		icon = mission_cannons_firing
		position = 3
		required_missions = {
			eran_south_way
			eran_zoroaster_temples
		}
		provinces_to_highlight = {  } 

		trigger = {
			if = {
				limit = {
					has_government_mechanic = firenation_mechanic
				}
				has_government_power = {
					mechanic_type = firenation_mechanic
					power_type = fire_nation
					value = 50
				}
			}else = {
				mil_power = 600
			}
			num_of_artillery = 10
		}

		effect = { add_years_of_income = 1 } 
	}

	eran_fired_up = {
		icon = per_balance_the_three_flames
		position = 4
		required_missions = { eran_fires_up } 
		provinces_to_highlight = {  } 

		trigger = {
			if = {
				limit = {
					has_government_mechanic = firenation_mechanic
				}
				has_government_power = {
					mechanic_type = firenation_mechanic
					power_type = fire_nation
					value = 100
				}
			}else = {
				mil_power = 600
			}
			num_of_owned_provinces_with = {
				value = 15
				religion = zoroastrian
				artillery_in_province = 2
			}
		}

		effect = {
			if = {
				limit = {
					has_government_mechanic = firenation_mechanic
				}
			}else = {
				add_mil_power = -600
			}
			add_country_modifier = {
				name = "eran_cannons"
				duration = -1
			}
		}
	}
}
PR1_5_slot = {
	slot = 5
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		OR = {
			tag = PR1
			AND = {
				tag = PER
				was_tag = PR1
			}
		}
	}

	eran_two_falls = {
		icon = per_qizilbash_loyalty
		position = 1
		provinces_to_highlight = { is_capital_of = PR0 } 

		trigger = {
			OR = {
				has_won_war_against = { who = PR0 } 
				stability = 3
			}
		}

		effect = {
			add_government_power = {
				mechanic_type = firenation_mechanic
				power_type = fire_nation
				value = 10
			}
			khorasan_region = { add_permanent_claim = ROOT } 
		}
	}

	eran_the_other_falls = {
		icon = mission_restore_trade_superiority
		position = 2
		required_missions = { eran_two_falls } 
		provinces_to_highlight = {
			region = khorasan_region
			NOT = { country_or_non_sovereign_subject_holds = ROOT } 
		}

		trigger = {
			num_of_owned_provinces_with = {
				value = 25
				region = khorasan_region
				religion = zoroastrian
			}
		}

		effect = {
			add_country_modifier = {
				name = "eran_more_zoro"
				duration = -1
			}
		}
	}

	eran_push_east = {
		icon = mam_persia
		position = 3
		required_missions = { eran_the_other_falls } 
		provinces_to_highlight = {
			region = khorasan_region
			NOT = { country_or_non_sovereign_subject_holds = ROOT } 
		}

		trigger = {
			num_of_provinces_owned_or_owned_by_subjects_with = {
				value = 25
				region = khorasan_region
				religion = zoroastrian
				has_tax_building_trigger = yes
			}
		}

		effect = {
			set_country_flag = eran_enemies_on_fire
			custom_tooltip = ERAN_TT_1
			tooltip = {
				add_country_modifier = {
					name = "eran_at_war"
					duration = -1
				}
			}
		}
	}
}
