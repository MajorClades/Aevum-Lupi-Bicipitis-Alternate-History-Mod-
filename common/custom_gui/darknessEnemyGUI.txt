
custom_text_box = {
    name = darkness_enemy_text_title #Must match a scripted instantTextBoxType in a .gui file
    potential = { 
		religion = jyotisastra
	} #Determines when the text is visible
    #To localize it, use the name of the button as a key, can also use customizable localisation
}

custom_icon = {
    name = darkness_enemy_bg #Must match a scripted iconType in a .gui file
    potential = { 
		religion = jyotisastra
	} #Determines when the icon is visible
}


custom_shield = {
    name = darkness_enemy_shield #Must match a scripted guiButtonType in a .gui file
    potential = { 
		religion = jyotisastra
	} #Determines when the shield is visible
    trigger = { 
		hidden_trigger = {
			always = yes
		}
	} #Determines when the shield is clickable
    effect = { 
      custom_tooltip = WAR_DARKNESS
      tooltip = {
        add_country_modifier = {
          name = war_against_darkness_jyo
          duration = -1
        }
      }

    } #Effect for clicking
    tooltip = is_darkness_enemy_tt #Can use customizable localisation
    global_event_target = darkness_enemy_target
    open_country = yes #If clicking opens the country view
}