pseudo_druidism_adm_order = {
    icon = GFX_holy_order_ast_adm_order
    trigger = {
        religion = pseudo_druidism
    }
    localization = holy_order
    cost = 50
    cost_type = adm_power
    per_province_effect = {
        add_base_tax = 1
    }
    per_province_abandon_effect = {
        add_base_tax = -1
    }
    modifier = {
        local_missionary_strength = 0.015
        local_governing_cost_increase = -5
    }
    ai_priority = {
        factor = 2
        modifier = {
            factor = 2.0
            NOT = { religion = ROOT }
        }
    }
    color = {
        100 5 50
    }
}

pseudo_druidism_dip_order = {
    icon = GFX_holy_order_ast_dip_order
    trigger = {
        religion = pseudo_druidism
    }
    localization = holy_order
    cost = 50
    cost_type = dip_power
    per_province_effect = {
        add_base_production = 1
    }
    per_province_abandon_effect = {
        add_base_production = -1
    }
    modifier = {
        local_culture_conversion_cost = -0.25
        local_build_cost = -0.15
    }
    ai_priority = {
        factor = 1
        modifier = {
            factor = 2.0
            NOT = { culture = ROOT }
        }
    }
    color = {
        50 5 100
    }
}

pseudo_druidism_mil_order = {
    icon = GFX_holy_order_ast_mil_order
    trigger = {
        religion = pseudo_druidism
    }
    localization = holy_order
    cost = 50
    cost_type = mil_power
    per_province_effect = {
        add_base_manpower = 1
    }
    per_province_abandon_effect = {
        add_base_manpower = -1
    }
    modifier = {
        local_unrest = -2
        local_defensiveness = 0.1
    }
    ai_priority = {
        factor = 1
        modifier = {
            factor = 1.5
            unrest = 5
        }
        modifier = {
            factor = 1.5
            devastation = 50
        }
    }
    color = {
        100 150 100
    }
}
